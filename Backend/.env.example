PORT=5000
FRONTEND_ORIGIN=http://localhost:5173
JWT_SECRET=replace-with-a-long-random-secret
SEED_CSV=../GigCred_synthetic_10_users.csv
GCS_BUCKET=

# Firestore uses Google Application Default Credentials.
# Set GOOGLE_APPLICATION_CREDENTIALS to your Firebase service-account JSON path,
# or run this backend in an environment with an attached service account.
# GOOGLE_APPLICATION_CREDENTIALS=C:/path/to/firebase-service-account.json
